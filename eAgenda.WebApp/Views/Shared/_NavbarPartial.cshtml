@{
    var rotaContatosAtiva = ViewContext.HttpContext.Request.Path.StartsWithSegments("/contatos") ? "nav-link active btn btn-primary px-3 text-white" : "nav-link text-primary fw-semibold";
    var rotaCompromissosAtiva = ViewContext.HttpContext.Request.Path.StartsWithSegments("/compromissos") ? "nav-link active btn btn-primary px-3 text-white" : "nav-link text-primary fw-semibold";
    var rotaCategoriasAtiva = ViewContext.HttpContext.Request.Path.StartsWithSegments("/categorias") ? "nav-link active btn btn-primary px-3 text-white" : "nav-link text-primary fw-semibold";
    var rotaDespesasAtiva = ViewContext.HttpContext.Request.Path.StartsWithSegments("/despesas") ? "nav-link active btn btn-primary px-3 text-white" : "nav-link text-primary fw-semibold";
    var rotaTarefasAtiva = ViewContext.HttpContext.Request.Path.StartsWithSegments("/tarefas") ? "nav-link active btn btn-primary px-3 text-white" : "nav-link text-primary fw-semibold";
}

<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container">
        <a class="navbar-brand fs-4 fw-bold" href="/">e-Agenda</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto gap-2">
                <li class="nav-item">
                    <a class="@rotaContatosAtiva" href="/contatos">Contatos</a>
                </li>
                <li class="nav-item">
                    <a class="@rotaCompromissosAtiva" href="/compromissos">Compromissos</a>
                </li>
                <li class="nav-item">
                    <a class="@rotaCategoriasAtiva" href="/categorias">Categorias</a>
                </li>
                <li class="nav-item">
                    <a class="@rotaDespesasAtiva" href="/despesas">Despesas</a>
                </li>
                <li class="nav-item">
                    <a class="@rotaTarefasAtiva" href="/tarefas">Tarefas</a>
                </li>
            </ul>
        </div>
    </div>
</nav>
